fastapi==0.115.8 # Framework web principal para construir a API (Versão corrigida sem vulnerabilidades)
uvicorn==0.27.0 # Servidor ASGI para rodar o FastAPI
sqlalchemy==2.0.25 # ORM para mapeamento e manipulação do banco de dados
psycopg2-binary==2.9.9 # Driver para conexão com o PostgreSQL
pydantic==2.5.3 # Validação de dados e definição de esquemas
apscheduler==3.10.4 # Agendador de tarefas em segundo plano (disparo de mensagens)
httpx==0.26.0 # Cliente HTTP para enviar os Webhooks para o N8N
python-dotenv==1.2.2 # Carregar variáveis de ambiente do arquivo .env (Versão com correção de symlinks)
boto3==1.34.34 # SDK da AWS (usado para Backblaze S3) para upload de mídias
python-multipart==0.0.31 # Necessário para processar uploads de arquivos no FastAPI (Versão corrigida para DoS)
pytz==2024.1 # Suporte a fuso horário para horário de Brasília
slowapi==0.1.9 # Implementação de Rate Limiting para FastAPI
redis==5.0.1 # Cliente Redis para armazenamento do Rate Limiting
passlib==1.7.4 # Hashing de senhas
bcrypt==4.0.1 # Criptografia de senhas
argon2-cffi==23.1.0 # Algoritmo Argon2id Memory-Hard para proteção anti-GPU de senhas
python-jose[cryptography]==3.4.0 # Criação e validação de tokens JWT (Versão corrigida para ECDSA)

# Testes automatizados
pytest==8.3.2
pytest-asyncio==0.23.5
respx==0.20.2

# Mensageria e Filas
pika==1.3.2 # Cliente para comunicação com RabbitMQ (fila AMQP)

# Qualidade e Segurança de Código
flake8==7.3.0 # Ferramenta de linting e análise estática de código Python
pip-audit==2.10.1 # Auditoria e verificação de vulnerabilidades conhecidas (CVEs) em pacotes Python

# Extração de texto de arquivos para importação de mensagens (PDF e DOCX)
pypdf==5.1.0 # Leitura e extração de texto de arquivos PDF
python-docx==1.1.2 # Leitura e extração de texto de arquivos DOCX

